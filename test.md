Test task() alleen:


lion '"Build a simple REST API with user CRUD endpoints using FastAPI" -> task(3) -> pride(2)'
2. Test context() alleen:


lion '"Refactor the display module to support themes" -> context() -> pride(2)'
3. Test task() + context() samen:


lion '"Build a CLI todo app with add, list, complete, and delete commands" -> task(4) -> context() -> pride(2) -> test()'
4. De ultieme test (alles samen):


lion '"Build a markdown note-taking CLI tool with: 1) create/edit/delete notes, 2) search by title/content, 3) tag system, 4) export to HTML" -> task(4) -> pride(2) -> test()'
Ik zou beginnen met #1 om te zien of task() correct opsplitst 